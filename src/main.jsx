import React from "react";
import ReactDOM from "react-dom/client";
import BloomApp from "./BloomApp.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <BloomApp />
  </React.StrictMode>
);
